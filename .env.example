# Reference only; the application deliberately does not auto-load .env files.
# Export these in your shell. Never put credentials here.
AWS_PROFILE=drift-demo
DRIFT_AWS_REGION=ap-southeast-2
DRIFT_BASELINE_PATH=.baseline/security_baseline.json
DRIFT_ENABLE_CLOUDTRAIL=false
DRIFT_LOG_LEVEL=WARNING
